# AI_RULES.md — AI推論ルール（Appathy共通）

TubeDesk / TubeCut で使うが、**他のAppathyアプリにも流用できる形**にしてある。
新しいアプリで推論を入れるときは、まずこの文書を読んでから設計すること。

- version: `1`
- 推論先: BonsaiApp のローカルLLM（OpenAI互換HTTPサーバ）
- 契約: `BONSAI_API.md`（所有者は BonsaiApp 側チャット。こちらは読むだけ）

---

## 1. 前提となる制約

**実測 約3.9 tok/s（4Bモデル）。** これが全ての設計を決める。

| 出力量 | 所要時間 |
|---|---|
| 20トークン | 約5秒 |
| 60トークン | 約15秒 |
| 150トークン | 約40秒 |
| 400トークン | 約100秒 |

さらに端末上での推論は発熱し、他アプリの動作にも影響する。

### ここから導かれる原則

> **推論を主役にしない。決定的な生成を主役にし、推論は上乗せにする。**

テンプレート合成で8割の価値が出るなら、それを既定にして即座に返す。
推論は「もっといい案が欲しい」と利用者が明示的に押したときだけ走らせる。

---

## 2. 五つのルール

### R1. 決定的生成が先、推論は後

全ての生成機能は、**推論なしで動く決定的な実装を必ず持つ**。
推論はその上に足すもので、置き換えるものではない。

```text
[生成]ボタン        → テンプレ合成。即座に結果が出る
[AIで作り直す]ボタン → 推論。押した人だけが待つ
```

BonsaiApp が未インストール・未起動・応答なしのいずれでも、
アプリは何事もなかったように動き続けること。**エラーダイアログを出さない。**

### R2. 出力は極小に絞る

1回の推論で出させるのは **60トークン以内**を目標、**150トークンを上限**とする。
超える設計になったら、タスクを分割するか諦める。

- 長文（説明文全体・台本全文）を一度に生成させない
- 生成させるのは「候補の断片」。組み立てはアプリ側の決定的処理でやる
- ストリーミングで受け、1行できるたびに画面へ足す（待ち時間の体感を下げる）

### R3. 出力はJSON固定。壊れていたら捨てる

```text
system: 出力はJSONのみ。前置き・説明・コードフェンス禁止。
```

- 受信後、必ずスキーマ検証する
- 検証に落ちたら **1回だけ再試行**し、それも落ちたら決定的生成の結果を使う
- 落ちたことを利用者に伝えない（結果は出ているので問題にならない）
- コードフェンス（```）の除去は前処理で必ず行う

### R4. 自動適用しない。必ず候補として出す

推論結果を本文へ直接書き込まない。**タップで採用する候補**として並べる。

- 候補は3〜6件
- 元の内容は候補を採用するまで消えない
- 採用後に取り消せる（1手だけ戻せれば十分）

理由は2つ。生成物の品質が安定しないこと。そして
**自分で選んだものは自分の作品だと感じられる**こと。後者のほうが重要。

### R5. 生成物には出所を残す

保存するデータに `origin` を持たせる。

```text
origin: "manual" | "template" | "ai"
```

- 画面上では区別しない（利用者に意識させない）
- バックアップJSONには残す
- 「AI生成のまま手を入れていない文章」がどれだけ残っているかを後から把握できる

---

## 3. タスク定義の書き方

推論を使う機能は、必ず以下の形で定義してから実装する。
**この表が埋まらないなら、その機能に推論は要らない。**

| 項目 | 内容 |
|---|---|
| タスクID | `AI-xx` |
| 決定的版 | 推論なしでどう作るか（**必須**） |
| 入力 | プロンプトに渡すもの。最小限に |
| 出力スキーマ | JSON |
| 想定トークン | 60以内が目標 |
| 失敗時 | 決定的版にフォールバック |

---

## 4. タスク一覧

### AI-01 タイトル候補

| | |
|---|---|
| 決定的版 | テンプレ6種に作品タイトルを差し込む（`【保存版】〜` 等） |
| 入力 | 作品タイトル、型、台本の最初の80字 |
| 出力 | `{"c":["候補1","候補2","候補3"]}` |
| 想定 | 45トークン |
| 制約 | 各候補28字以内 |

### AI-02 説明文の冒頭2〜3行

| | |
|---|---|
| 決定的版 | 台本の最初のシーン本文を60字で切る |
| 入力 | タイトル、台本の最初の120字 |
| 出力 | `{"lead":"…"}` |
| 想定 | 50トークン |
| 備考 | **説明文全体は生成させない。** チャプター・定型文はアプリ側で組む |

### AI-03 タグ / ハッシュタグ

| | |
|---|---|
| 決定的版 | タイトルと副題を区切り文字で分割し、型ごとの固定タグを足す |
| 入力 | タイトル、副題、型 |
| 出力 | `{"t":["タグ1","タグ2",…]}` |
| 想定 | 35トークン |

### AI-04 サムネの文言

| | |
|---|---|
| 決定的版 | 作品タイトルをそのまま入れ、副題は台本冒頭28字 |
| 入力 | タイトル、台本の最初の80字、様式 |
| 出力 | `{"main":"…","sub":"…"}` |
| 想定 | 30トークン |
| 制約 | 主題12字以内、副題28字以内。**縦書き様式では8〜12字が最も映える** |

### AI-05 テロップ文言（TubeCut）

| | |
|---|---|
| 決定的版 | 対応する Scene の `head` をそのまま使う |
| 入力 | 区間のラベル、対応する Scene の本文80字 |
| 出力 | `{"t":"…"}` |
| 想定 | 20トークン |
| 制約 | 16字以内 |

### AI-06 シーン見出しの提案

| | |
|---|---|
| 決定的版 | 型ごとのテンプレ見出し（掴み→結論→理由①…） |
| 入力 | シーン本文の最初の60字 |
| 出力 | `{"h":"…"}` |
| 想定 | 15トークン |
| 制約 | 10字以内 |

---

## 5. 呼び出しの実装規約

### 5.1 パラメータ

```text
temperature  0.6   （候補生成なので既定の0.35より上げる）
max_tokens   タスクごとの想定 × 1.5
timeout      45秒
retry        1回のみ。同じプロンプトで
```

タイムアウトしたら黙って決定的版に落ちる。

### 5.2 プロンプトの組み方

```text
system: あなたは短い日本語の候補を作る。出力はJSONのみ。
        前置き・説明・コードフェンスは書かない。
user:   {タスク固有の指示}
        {入力データ}
        形式: {"c":["…","…","…"]}
```

- system は毎回同じ。タスク差はすべて user 側に置く
- 形式の見本を毎回入れる。これだけで壊れ方が大幅に減る
- **入力は必要最小限。** 台本全文を渡さない。冒頭80〜120字で足りる

### 5.3 入れてはいけないもの

- トークン・URL・個人名・メールアドレス
- 利用者が書いた文章の全文（要約や冒頭に切る）
- 他の作品のデータ

### 5.4 UIの作法

```text
[AIで作り直す]  → 押すとボタンが「生成中…（12秒）」に変わる
                  経過秒数を出す。プログレスバーは使わない（進捗が読めないため）
                  もう一度押すと中断
```

- 生成中も他の操作を止めない
- 結果は既存の候補リストの**上に足す**。既存を消さない
- 2回押されたら前の要求は捨てる

---

## 6. 推論を使わないと決めた箇所

判断の記録として残す。**同じ議論を繰り返さないため。**

| 機能 | 推論を使わない理由 |
|---|---|
| 台本全文の生成 | 400トークン超＝2分待ち。かつ本人の言葉でないと喋りにくい |
| 説明文の全体 | チャプターと定型文が大半。テンプレで足りる |
| 無音カットの判断 | RMS閾値で決定的に出せる。推論の入る余地がない |
| キーフレーム吸着 | 完全に機械的な計算 |
| サムネの構図 | レイアウト固定が方針。生成する対象がない |
| 実績の分析コメント | データが数件では意味のあることが言えない |

---

## 7. 新しいアプリで使うときの手順

1. この文書をリポジトリ直下にコピーする
2. `BONSAI_API.md` も置く（BonsaiApp側チャットが所有）
3. 3章の表を埋めてタスクを定義する。**決定的版が書けないタスクは作らない**
4. 決定的版を先に実装して動かす
5. その後で推論を上乗せする

**4と5を逆にしない。** 推論から作ると、BonsaiApp が動かない環境で
アプリ全体が使えなくなる作りになる。

---

## 8. 実装状況（TubeHQApp v3.1）

`Bonsai.kt` を Desk / Cut の両方に置いた。共通部分（`ask` / `parse` / `call` / 接続先設定）は同一で、
タスク定義だけが違う。他アプリへ持っていくときは共通部分をそのままコピーしてタスクを差し替える。

| タスク | 置き場所 | 決定的版 |
|---|---|---|
| AI-01 タイトル候補 | Desk メタタブ「AIで作り直す」 | テンプレ6種 |
| AI-02 説明文の冒頭 | Desk メタタブ「冒頭をAIで」 | 台本冒頭60字 |
| AI-03 タグ | Desk メタタブ「AIで足す」 | 区切り分割＋型別固定タグ |
| AI-04 サムネ文言 | Desk 画像タブ「文言をAIで」 | 台本から流し込む |
| AI-05 テロップ文言 | Cut 重ねタブ「文言をAIで」 | 区間ラベルをそのまま |
| AI-06 シーン見出し | Desk 台本タブ「見出しAI」 | 型別テンプレ見出し |

接続先は各アプリの「AI接続先」から設定する。既定は `http://127.0.0.1:8080`。
`INTERNET` 権限と `usesCleartextTraffic="true"` が必要（ローカルホスト宛のため）。

### 実装で守っていること
- 生成中はボタンの文字が「生成中… N秒」に変わる。もう一度押すと中断
- AI の候補は既存候補の**上に足す**。既存は消さない
- 失敗しても toast を一度出すだけ。ダイアログもログも出さない
- タグは既存とマージして重複を除く

---

## 9. 変更履歴

| version | 日付 | 内容 |
|---|---|---|
| 1 | 2026-08-12 | 初版。4Bモデル 3.9 tok/s を前提に策定 |
