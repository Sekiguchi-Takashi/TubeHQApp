# TubeCut 機能設計

`CUT_ONTOLOGY.md` をスマホ上で動く機能に落としたもの。
**端末で実際に動くかを唯一の判断基準にしている。**

---

## 1. 設計の柱：2つの実行レーン

編集内容によって処理の重さが桁違いに変わる。これを利用者に隠さず、**常に表示する**。

| | 速いレーン | 重いレーン |
|---|---|---|
| 実装 | MediaExtractor + MediaMuxer（アプリ内） | Termux の ffmpeg |
| できること | カット・結合・並べ替えのみ | テロップ・BGM・縦切り出し・音量正規化 |
| 画質 | 無劣化（再エンコードしない） | 再エンコードで劣化 |
| 10分尺の所要 | 5〜15秒 | 10〜30分 |
| 発熱 | ほぼなし | 大きい |
| 前提条件 | 全素材の codec / 解像度 / fps が一致 | なし |

**出力タブに常時この判定を出す。**
「今の編集内容ならアプリ内で約12秒」／「テロップがあるため ffmpeg が必要（推定8分）」

これが本アプリの中心的な価値。編集の重さを事前に知れることで、
撮り直しか編集で直すかの判断ができる。

---

## 2. キーフレーム吸着（最重要の制約）

MediaMuxer による無劣化カットは、**切れる位置がキーフレームに限られる**。
任意の位置で切りたければ再エンコードが必須になる。

### 挙動

- カット点を指定すると、直近のキーフレームに自動で吸着する
- 吸着によるズレを毎回表示する（例：`-0.４秒ズレます`）
- ズレが許容できない場合のために、その場で「ffmpegで正確に切る」へ切り替えられる
- キーフレーム位置は `MediaExtractor.seekTo(SEEK_TO_PREVIOUS_SYNC)` で走査して事前にキャッシュ

### なぜ隠さないか

多くの編集アプリはここを黙って再エンコードで解決し、結果として遅く・熱く・劣化する。
無音カット主体の一人喋り動画なら、0.5秒のズレはほぼ問題にならない。
**選ばせることで速いレーンに留まれる。**

---

## 3. 解析機能

### 3.1 プローブ（素材の素性を読む）

`MediaExtractor` + `MediaFormat` で取得。取り込み直後に自動実行。

| 取得項目 | 用途 |
|---|---|
| 実尺 | EditPlan の実測値 |
| 映像codec / プロファイル | 速いレーンの可否判定 |
| 解像度 / fps | 同上 |
| 回転情報 | プレビューの向き補正 |
| 音声codec / サンプルレート | 無音検出の前提 |

複数素材を取り込んだ時点で**一致判定**を行い、速いレーンが使えるかを素材タブに出す。
不一致なら、どのファイルのどの項目が食い違っているかを名指しで表示する。

### 3.2 無音検出

```text
MediaExtractor（音声トラック）
  → MediaCodec でデコード
  → PCM を 20ms 窓で RMS 算出
  → 閾値以下が最短長以上続く区間を「無音」とする
```

- 10分尺で概ね10〜25秒。バックグラウンドスレッド＋進捗表示（通知は出さない）
- RMS 配列は 50個/秒 で保持（10分＝30,000要素）。JSON に保存せずキャッシュファイルに置く
- 調整できるのは2つだけ：**閾値**（dB）と**最短無音長**（秒）
- 変更するとカット候補が即座に再計算される（RMS配列は再利用するので一瞬）

**前後マージン**を設ける。無音の始まりぴったりで切ると語尾が欠けるため、
既定で開始側 +0.15秒 / 終了側 −0.10秒 を残す。ここも調整可能にする。

### 3.3 波形表示

RMS 配列を Canvas に描く。表示幅に合わせて間引き（最大値を採る）。
無音区間を暗く、カット候補の境界を縦線で重ねる。

**波形は編集の主UIにしない。** スマホの横幅では10分尺を精密操作できない。
波形は全体の見取り図として上部に固定表示し、実際の操作はリスト（3.4）で行う。

### 3.4 カット候補リスト（Cut の主UI）

無音検出の結果を **区間のリスト** として出す。1行1区間。

```
[✓] 00:00 - 00:18   18秒   「掴み」
[ ] 00:18 - 00:21    3秒   （無音）
[✓] 00:21 - 01:04   43秒
```

- チェックを外した区間が捨てられる。無音区間は既定で外れている
- 行をタップすると、その区間の先頭フレームと in/out の微調整が出る
- 微調整は **±0.1秒ボタン**で行う。スライダーは指では精密に扱えない
- 長押しで区間の分割・結合

この「リストで選ぶ」形にすることで、タイムラインの横スクロールを避けられる。
スマホで編集する上で最も効く設計判断。

### 3.5 フレーム抽出

`MediaMetadataRetriever.getFrameAtTime` は1枚あたり100〜300ms かかる。
**フィルムストリップは作らない。** 区間ごとに先頭1枚だけを遅延取得してキャッシュする。

Desk へのサムネイル素材受け渡しもここから行う。

---

## 4. 編集の指定

### 4.1 並べ（Segment）

- 区間の並べ替え（↑↓ボタン。ドラッグは誤操作が多い）
- 複数テイクの取り込みと連結
- Desk から来た `Scene` との対応付け。`head` が区間ラベルの初期値になる
- 対応が崩れた区間は `sceneId = null` として扱う

### 4.2 テロップ

**Canvas で日本語テロップPNGを生成し、ffmpeg の overlay で乗せる。**
`drawtext` はフォント指定と改行の扱いが面倒なので使わない。

- 区間ごとに、相対時刻と表示秒数を指定
- 位置は 下 / 中央 / 上 の3択（自由配置にしない。スマホでは調整が苦痛）
- 文字サイズ・色・フチ幅はプリセット4種から選ぶ
- Desk 側の `Scene.head` / `body` の冒頭を候補として出す

### 4.3 音

- BGM の区間と音量
- `loudnorm` の有無（既定でオン。スマホ収録の音量ばらつきを吸収する）
- BGM のフェードイン / アウト秒数

### 4.4 画角

- 縦切り出し（1080×1920）の枠指定
- 枠は横位置のみ動かせる（中央 / 左 / 右 / 数値）。縦は全高固定
- プレビューは抽出フレーム上に枠を重ねて確認する

---

## 5. 実行

### 5.1 速いレーン（アプリ内）

```text
MediaExtractor で区間ごとにサンプルを読み
MediaMuxer に書き出す
  → 映像・音声トラックをそのままコピー
  → タイムスタンプを詰め直す
```

進捗をプログレスバーで表示。中断可能。

### 5.2 重いレーン（Termux）

```text
TubeCut ── plan.json + telop_*.png ──→ 共有ディレクトリ
        ── run_<id>.sh を書き出し ──→ 同上
        ── 完了ファイルをポーリング ←── ffmpeg 実行
```

**Termux の起動方式は2通り用意する。**

| 方式 | 前提 | 扱い |
|---|---|---|
| RUN_COMMAND インテント | `com.termux.permission.RUN_COMMAND` の付与と Termux 側の `allow-external-apps=true` | 設定できた人向け |
| スクリプト書き出し＋コマンドのコピー | なし | **既定。** 確実に動く |

既定を後者にするのは、前者が Termux の設定変更を要求し、失敗時の原因究明が難しいため。
利用者は Termux に切り替えて貼り付けるだけで済む。

完了判定は、出力ファイルの存在とサイズが2回連続で変わらないことをもって行う。
ffmpeg のログをポーリングして進捗を拾う（`time=` 行）。

---

## 6. 画面構成

| タブ | 中身 |
|---|---|
| 素材 | 取り込み、プローブ結果、**速いレーン可否の判定** |
| 解析 | 無音検出の実行、閾値と最短長の調整、波形、進捗 |
| 区間 | カット候補リスト。チェック・並べ替え・微調整（**主UI**） |
| 重ね | テロップ・BGM・縦切り出し |
| 出力 | **レーン判定と所要時間の見積り**、実行、EditPlan書き出し |

---

## 7. データ

- 保存先: `filesDir/tubecut.json`
- RMS 配列とフレームキャッシュは `cacheDir` に置き、JSON には入れない
- 素材の URI は永続化権限を取得して保持（`takePersistableUriPermission`）

---

## 8. 実装順

1. プローブ＋一致判定（レーン判定の土台）
2. MediaMuxer による無劣化カット・結合（速いレーンだけで完結する最小構成）
3. 無音検出＋カット候補リスト
4. EditPlan の入出力（Desk 連携）
5. ffmpeg コマンド生成とスクリプト書き出し
6. テロップ・BGM・縦切り出し

**1〜3 だけで一人喋り動画の実用に足りる。** ここまでを最初の目標にする。

---

## 9. やらないこと

- リアルタイムプレビュー再生（抽出フレームの静止画で代用）
- タイムラインの横スクロール操作（区間リストで代替）
- フィルムストリップ表示（フレーム抽出が遅い）
- トランジション・エフェクト（重いレーン専用になり、割に合わない）
- 企画・台本・メタデータ・投稿（全て Desk）
