# 画面設計 — TubeDesk / TubeCut

`DESK_FUNCTIONS.md` / `CUT_FUNCTIONS.md` を画面に落としたもの。
UIは全てKotlinから生成する（XMLレイアウトなし）ため、この文書がレイアウトの唯一の定義になる。

---

## 0. 共通のUI規約（Appathy共通・他アプリにも適用可）

### 0.1 骨格

```text
┌─────────────────────────┐
│ ヘッダー（2行）          │  アプリ名 / 文脈行
├─────────────────────────┤
│                         │
│ 本文（ScrollView）       │  タブごとに差し替え
│                         │
├─────────────────────────┤
│ 下タブ（等幅・横スクロール禁止）│
└─────────────────────────┘
```

- ヘッダー2行目は**今どの対象を編集しているか**を必ず出す。
  対象未選択なら「未選択 / 全N件」
- 下タブは等幅。名前は2〜3文字に収める。5〜6個が上限
- タブが6個を超えるなら機能を削るか別アプリに切る。横スクロールタブは作らない

### 0.2 操作の原則

| 原則 | 理由 |
|---|---|
| 数値の微調整は **±ボタン**。スライダーは使わない | 指では0.1秒精度を出せない |
| 並べ替えは **↑↓ボタン**。ドラッグしない | スクロールと競合して誤操作する |
| 破壊的操作は AlertDialog で確認 | 取り消しがない |
| 主要ボタンは1画面に**1つだけ**アクセント色 | どれを押せばいいか迷わせない |
| 縦に長い画面の底には40dpの余白 | 下タブに隠れる |
| 保存ボタンを作らない。入力即保存 | 保存忘れが最大の事故 |

### 0.3 プレビューの置き方

画像や動画を扱う画面では、**上部にプレビューを固定して下に操作を並べる**。
プレビューは実寸ではなく縮小描画（0.3〜0.45倍）。
書き出し時のみ等倍で描き直す。

### 0.4 「次にやること」の原則

一覧画面には必ず、**次の1手だけを大きく出す領域**を最上部に置く。
選択肢を並べるのではなく、1件を名指しする。迷いを消すのが目的。

---

# 1. TubeDesk 画面設計

タブ：`ホーム` `ネタ` `台本` `画像` `メタ` `実績`

---

## 1.1 ホーム

```text
┌─────────────────────────┐
│ TubeDesk                │
│ 未選択 / 全12件          │
├─────────────────────────┤
│ ┏━━━━━━━━━━━━━━━━━━━━━┓ │
│ ┃ 次にやること          ┃ │  ← カード。最上部
│ ┃ カンペを見ながら撮影  ┃ │  ← 24sp・アクセント色
│ ┃ 「休日の過ごし方」    ┃ │
│ ┃ [この作品を編集] [次へ]┃ │
│ ┗━━━━━━━━━━━━━━━━━━━━━┛ │
│                         │
│ パイプライン            │
│ ●ネタ 3件               │  ← ステータス別に見出し＋件数
│  ┌───────────────────┐  │
│  │[絵] 作品名         │  │
│  │     一人喋り 08:42 │  │
│  │ [編集][カンペ][削除]│  │
│  └───────────────────┘  │
│ ●台本済 2件             │
│  …                      │
│                         │
│ [バックアップ][読み込み] │
└─────────────────────────┘
```

- 作品カードの左にサムネイル（`role=thumbnail` の採用分を0.10倍で描画）
- ステータスチップの色：ネタ=青 / 台本済=金 / 撮影待ち=橙 / 素材あり=紫 / 投稿=緑 / 完了=灰
- 「次へ」は status を1段進める。戻すのは作品カードの長押し

---

## 1.2 ネタ

```text
│ ネタ帳                  │
│ [思いついたことを1行で ] │
│ [補足メモ（任意）      ] │
│ 型                      │
│ [一人喋り][スライド][画面]│  ← 選択中はアクセント色
│ [ネタを追加]            │
│                         │
│ ストック                │
│  ┌───────────────────┐  │
│  │ ネタの1行           │  │
│  │ 補足メモ            │  │
│  │ [台本へ昇格][削除]  │  │
│  └───────────────────┘  │
```

「台本へ昇格」で型に応じたテンプレを流し込み、status を `script` にして台本タブへ移動する。

---

## 1.3 台本

```text
│ 台本                    │
│ [作品タイトル          ] │
│ 想定尺 08:42 / 2,769字 / 8シーン │  ← アクセント色・常時更新
│ [テンプレを流し込む][カンペ] │
│ 状態: 台本済  [次の工程へ] │
│                         │
│  ┌───────────────────┐  │
│  │ ①      約18秒      │  │
│  │ [見出し            ]│  │
│  │ [喋る内容（4行）   ]│  │
│  │ [画・小道具メモ    ]│  │
│  │ [↑][↓][削除]       │  │
│  └───────────────────┘  │
│  … シーン数だけ繰り返し   │
│ [＋ シーンを追加]        │
│ [Cutへ送る]              │  ← plan書き出し。status→撮影待ち
```

- 本文を打つたびに、そのシーンの「約N秒」と上部の合計が更新される
- Cut から result が返っていれば、想定尺の行が `実尺 08:32（推定 08:42）` に変わる

### カンペ（別Activity・全画面）

```text
┌─────────────────────────┐
│                         │
│      台本本文が          │  ← 白・太字・行間1.5・上下に大きな余白
│      下から上へ流れる    │
│                         │
├─────────────────────────┤
│ ▶再生中 速度1.4 字30    │
│ [速−][再生][速+][字−][字+][頭出し]│
└─────────────────────────┘
```

画面タップで停止／再開。画面消灯を抑止。

---

## 1.4 画像

```text
│ [A案][B案]              │  ← role=thumbnail は2枚まで
│ ┌─────────────────────┐ │
│ │                     │ │  ← プレビュー（0.42倍）
│ │      固定           │ │
│ └─────────────────────┘ │
│ 様式                    │
│ [サムネ][予告][ショート][プレイヤー]│
│ [背景を選ぶ][背景なし]   │
│ [Cutのフレームを使う]    │  ← 抽出フレームを背景に
│                         │
│ 文字                    │  ← 様式ごとに項目が変わる
│ [テロップ              ] │
│ [サブ文字              ] │
│ …                       │
│                         │
│ 色味                    │
│ [色見本8色]             │
│ 明るさ  [−][ 0 ][+]      │  ← ±ボタン
│ コントラスト [−][ 0 ][+] │
│ …                       │
│ [台本から流し込む]       │
│ [PNGで書き出す]          │
│ [この案を採用]           │
```

- 様式を切り替えても文字スロットは保持する（共通スロットのため）
- 「採用」した案がホームのカードと一覧のサムネイルになる

---

## 1.5 メタ

```text
│ タイトル                │
│ [動画タイトル          ] │
│ 候補（タップで採用）     │
│ [候補1][候補2]…         │  ← 縦に6個
│                         │
│ 説明文                  │
│ [説明文（8行）         ] │
│ [説明文を生成][チャプターのみコピー]│
│                         │
│ タグ                    │
│ [タグ（カンマ区切り）  ] │
│ [タグを生成]            │
│                         │
│ [タイトルをコピー][説明文をコピー]│
│ [タグをコピー]           │
```

チャプターは実尺があればそちら、なければ想定尺から生成する。
どちらを使ったかを説明文の上に小さく出す。

---

## 1.6 実績

```text
│ 公開                    │
│ [公開URL               ] │
│ [公開日時              ] │
│                         │
│ 数字（手入力）           │
│ 再生数    [        ]    │
│ 高評価    [        ]    │
│ コメント  [        ]    │
│ 視聴維持率[        ]%   │
│ [記録する]              │
│                         │
│ 履歴                    │
│  2026-08-12  1,204回     │
│  2026-08-15  3,880回     │
```

記録は追記式。折れ線グラフは作らない（数値の羅列で足りる）。

---

# 2. TubeCut 画面設計

タブ：`素材` `解析` `区間` `重ね` `出力`

---

## 2.1 素材

```text
│ TubeCut                 │
│ 休日の過ごし方 / 3ファイル│
├─────────────────────────┤
│ [ファイルを追加]         │
│                         │
│  ┌───────────────────┐  │
│  │[絵] VID_0012.mp4   │  │
│  │ 08:42 h264 1080p 30fps│  │
│  │ [↑][↓][削除]       │  │
│  └───────────────────┘  │
│  …                      │
│                         │
│ ┏━━━━━━━━━━━━━━━━━━━━━┓ │
│ ┃ 速いレーンが使えます   ┃ │  ← 緑。判定結果を常時表示
│ ┃ 全3件のcodec/解像度/fps┃ │
│ ┃ が一致しています       ┃ │
│ ┗━━━━━━━━━━━━━━━━━━━━━┛ │
```

不一致の場合は赤で、**どのファイルのどの項目が違うか**を名指しする。

```text
┃ ffmpegが必要です
┃ VID_0015.mp4 の fps が 60（他は30）
```

---

## 2.2 解析

```text
│ 無音検出                │
│ 閾値      [−][ -38dB ][+]│
│ 最短無音長 [−][ 0.6秒 ][+]│
│ 前マージン [−][ 0.15秒][+]│
│ 後マージン [−][ 0.10秒][+]│
│ [検出を実行]            │
│                         │
│ ▓▓░░▓▓▓░░░▓▓▓▓░▓▓      │  ← 波形（全尺を横幅に圧縮）
│ 　│　　│　　　│         │  ← カット候補の境界
│                         │
│ 検出結果                │
│ 24区間 / うち無音 11区間 │
│ 採用後の想定尺 06:12     │
│ （元 08:42 から 2:30短縮）│
```

- 実行は1回。閾値を変えると RMS 配列を再利用して即座に再計算する
- 波形は見取り図。ここでは操作しない

---

## 2.3 区間（主UI）

```text
│ 24区間 / 採用13 / 06:12  │
│ [無音を全て外す][全て採用]│
│                         │
│ ☑ 00:00-00:18  18秒     │
│    掴み                 │
│ ☐ 00:18-00:21   3秒 無音 │
│ ☑ 00:21-01:04  43秒     │
│    結論                 │
│ ☐ 01:04-01:07   3秒 無音 │
│ …                       │
```

行タップで展開：

```text
│ ☑ 00:21-01:04  43秒     │
│  ┌───────────────────┐  │
│  │ [フレーム画像]     │  │
│  │ 開始 [−][00:21.4][+]│  │  ← ±0.1秒
│  │ 終了 [−][01:04.2][+]│  │
│  │ ⚠ 開始が0.4秒ズレます│  │  ← キーフレーム吸着の警告
│  │ [ラベル            ]│  │
│  │ [↑][↓][分割][結合] │  │
│  └───────────────────┘  │
```

- キーフレーム吸着のズレは**必ず表示する**。ズレ0なら表示しない
- ズレが気になる場合の逃げ道として、警告の横に「正確に切る（ffmpeg）」を出す
- Desk から来た `Scene` があれば、ラベルの初期値に `head` が入る

---

## 2.4 重ね

```text
│ テロップ                │
│  ┌───────────────────┐  │
│  │ 区間③ に乗せる      │  │
│  │ [文言              ]│  │
│  │ 開始 [−][ 0.5秒 ][+]│  │  ← 区間内の相対秒
│  │ 表示 [−][ 3.0秒 ][+]│  │
│  │ 位置 [下][中][上]   │  │
│  │ 見た目 [太][細][白抜][帯]│ │
│  │ [削除]              │  │
│  └───────────────────┘  │
│ [＋ テロップを追加]      │
│                         │
│ BGM                     │
│ [音源を選ぶ]            │
│ 音量 [−][ 18% ][+]       │
│ フェードイン [−][2秒][+] │
│ 音量の自動調整 [オン][オフ]│
│                         │
│ 画角                    │
│ 縦切り出し [しない][する]│
│ 位置 [中央][左][右]      │
│ ┌─────────────────────┐ │
│ │  [枠付きプレビュー]  │ │
│ └─────────────────────┘ │
│                         │
│ ⚠ この画面の設定を使うと │
│   ffmpegが必要になります │
```

**最下部の警告が重要。** ここで何か設定した瞬間に重いレーンへ移ることを明示する。

---

## 2.5 出力

```text
│ ┏━━━━━━━━━━━━━━━━━━━━━┓ │
│ ┃ アプリ内で約12秒       ┃ │  ← 緑。または赤で
│ ┃ 無劣化・再エンコードなし┃ │     「ffmpegが必要（推定8分）」
│ ┗━━━━━━━━━━━━━━━━━━━━━┛ │
│                         │
│ 内訳                    │
│ 採用13区間 / 06:12       │
│ テロップ 0件            │
│ BGM なし                │
│ 縦切り出し なし          │
│                         │
│ [書き出す]              │  ← レーンに応じて動作が変わる
│                         │
│ ── 重いレーンの場合 ──   │
│ [スクリプトを書き出す]   │
│ [コマンドをコピー]       │
│ Termuxで貼り付けて実行   │
│ [完了を確認]            │
│                         │
│ [EditPlanを書き出す]     │
│ [Deskへ結果を返す]       │
```

- 実行中はプログレスバーと中断ボタン
- 重いレーンの進捗は ffmpeg ログの `time=` を拾って表示
- 完了後に「Deskへ結果を返す」が有効になる

---

## 3. 画面遷移

```text
Desk ホーム ──編集──→ 各タブ
     台本 ──Cutへ送る──→ （Cut側の素材タブに作品が現れる）

Cut  素材 → 解析 → 区間 → 重ね → 出力
     出力 ──Deskへ結果を返す──→ （Desk の status が edit→publish）
```

タブ間は自由に行き来できる。順番を強制しない。
ただしホームの「次にやること」は常に1つ先を指す。
