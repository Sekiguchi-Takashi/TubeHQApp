# TubeDesk 機能設計

`DESK_ONTOLOGY.md` をスマホ上で動く機能に落としたもの。

---

## 1. 既存コードの流用状況

v2.0 の 3,559行のうち、Desk 側は大半がそのまま使える。

| 機能 | 出自 | 状態 |
|---|---|---|
| パイプライン / ネタ帳 / 台本 / メタ | 旧 `app/`（TubeHQ） | ほぼそのまま |
| カンペ | 旧 `app/` PrompterActivity | そのまま |
| 静止画レンダラ | 旧 `shot/` Frames.kt + Yokoku.kt（786行） | **吸収する** |
| プリセット / 文言候補 | 旧 `shot/` Model.kt | 統合する |

**新規に書くのは以下だけ。**
- Work と Shot のデータ統合（2つのJSONを1つに）
- Publication / Analytics
- EditPlan の入出力

---

## 2. データ統合の方針

旧 TubeHQ の `Project` と旧 TubeShot の `Shot` を統合する。

```text
Work {
  id / title / type / status / created

  scenes[]      台本（旧 Project.scenes をそのまま）

  images[]      静止画（旧 Shot を配列で内包）
    ├ role      thumbnail | promo
    ├ style     thumb | yokoku | shorts | player
    ├ bg / textSlots / grade / accent
    └ 旧 Shot のフィールドをそのまま

  meta          タイトル / 説明 / タグ
  pub           公開日時 / URL / 実績値
  edit          EditPlan の往復状態
}
```

**`Shot` を `Work` の配下に入れるのが要点。** 旧 TubeShot は単体アプリだったため
1枚が独立していたが、Desk では「この作品のサムネA案」という位置づけになる。

`role` を持たせることで、サムネイル（本番用・1枚選ぶ）と宣伝画像（複数可）を
同じレンダラで扱いつつ区別できる。

---

## 3. 画面構成

| タブ | 中身 |
|---|---|
| ホーム | 「次にやること」1件を大きく表示 + ステータス別パイプライン |
| ネタ | 1行入力でストック、型を選んで追加、台本へ昇格 |
| 台本 | 見出し/本文/メモ、並べ替え、想定尺、カンペ起動、**Cutへ送る** |
| 画像 | 様式選択、背景、文字、色味、PNG書き出し、A/B比較 |
| メタ | タイトル候補、説明文生成、タグ、コピー |
| 実績 | 公開URL、再生数などの記録 |

タブが6つになるが、下部タブは横スクロールさせず等幅で収める（1文字〜3文字の短い名前を使う）。

---

## 4. ホーム：「次にやること」

Desk の中心はここ。**1件だけを大きく出す。**

| status | 表示される次の行動 |
|---|---|
| idea | 台本を書く |
| script | カンペを見ながら撮影 |
| shoot | 素材を Cut に送る |
| edit | Cut の結果を取り込む |
| publish | サムネとメタを仕上げて投稿 |
| done | 実績を記録する |

status の遷移は「次の工程へ」ボタン1つで進める。戻す操作は各作品カードから。

---

## 5. 台本まわり

### 想定尺の推定

`CPS = 5.3` 文字/秒。シーンごとと合計を常時表示。
Cut から result が返ったら、**実尺で上書きする**（推定値は括弧書きで残す）。

### カンペ

全画面・自動スクロール・速度可変・文字サイズ可変・画面消灯抑止。
画面タップで停止／再開。既存実装をそのまま使う。

### Cut へ送る

台本タブから `plan_<workId>.json` を書き出す。
送った時点で status を `shoot` に進める。

---

## 6. 静止画レンダラ

旧 TubeShot の Frames.kt / Yokoku.kt をそのまま移植。

| 様式 | 寸法 | 既定の role |
|---|---|---|
| `thumb` | 1280×720 | thumbnail |
| `yokoku` | 1280×720 | thumbnail |
| `shorts` | 1080×1920 | promo |
| `player` | 1920×1080 | promo |

- 文字は共通スロット。様式ごとに使うスロットが違うだけ。**様式別に分けない**
- `Frames.render(spec, bg, scale)` が唯一の描画入口。プレビュー0.42 / 一覧0.10 / 書き出し1.0
- 背景に Cut から受け取った抽出フレームを直接使えるようにする（これが統合の実利）
- **YouTubeのロゴ・ワードマークは描画しない**

### A/B比較

サムネイルは2枚まで並べて比較できる。片方を「採用」に指定すると、
ホームのカードのサムネイルがそれに切り替わる。

---

## 7. メタデータ

- タイトル候補6種（テンプレ合成）
- 説明文の生成（チャプター自動挿入）
- タグ / ハッシュタグ生成
- 全てクリップボードへコピー

**チャプターは2段階。** 台本段階では想定尺から、Cut の result 取り込み後は実尺から作り直す。
実尺版が存在する場合はそちらを既定にし、推定版は破棄する。

---

## 8. 投稿と実績

### v1 の範囲

投稿はしない。メタデータをコピーして Studio アプリに貼る。
公開後、URL と再生数を手入力で記録する。

### 将来 API を入れる場合の既知の落とし穴

- 未審査プロジェクトからAPIアップロードした動画は**強制的に private** になる
  「API で private 投稿 → Studioアプリで手動公開」が現実解
- quota は1日10000、`videos.insert` が1600（＝1日6本）
- OAuth は TV/限定入力デバイス用のデバイスコードフローなら外部SDK不要

手入力の実績記録を先に作っておけば、API を入れたときに表示側を作り直さずに済む。

---

## 9. データ

- 保存先: `filesDir/tubedesk.json`
- 背景画像・抽出フレームの URI は永続化権限を取得して保持
- バックアップ: SAF で JSON の書き出し / 読み込み

### 旧データの移行

旧 `tubehq.json` と `tubeshot.json` が存在すれば、初回起動時に読み込んで統合する。
`tubeshot.json` の Shot は `role = promo` の独立 Work として取り込む（対応する台本がないため）。
移行後も旧ファイルは消さない。

---

## 10. 実装順

1. データ統合（Work に Shot を内包）と旧データ移行
2. 既存画面の移植（パイプライン / ネタ / 台本 / カンペ / メタ）
3. レンダラの移植と画像タブ
4. EditPlan の入出力
5. 実績タブ

---

## 11. やらないこと

- 動画ファイルの読み込み・加工・書き出し（全て Cut）
- 音声の処理
- 実尺の自前計測（Cut から受け取る）
